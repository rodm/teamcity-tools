
task repackageMaven3_3(type: GradleBuild) {
    buildFile = 'maven3.3.gradle'
    tasks = ['clean', 'build']
}

task repackageMaven3_5(type: GradleBuild) {
    buildFile = 'maven3.5.gradle'
    tasks = ['clean', 'build']
}

task repackageFindBugs3_0(type: GradleBuild) {
    buildFile = 'findbugs3.gradle'
    tasks = ['clean', 'build']
}

task repackagePhantomJS2_1(type: GradleBuild) {
    buildFile = 'phantomjs.gradle'
    tasks = ['clean', 'build']
}
